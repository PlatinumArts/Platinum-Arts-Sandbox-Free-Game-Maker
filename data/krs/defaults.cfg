thirdperson 1

bind D [if $editing right steerright]
bind A [if $editing left steerleft]
bind W [if $editing forward accelerate]
bind S [if $editing backward reverse]

bind MOUSE1 [restart]
bind MOUSE2 [jump]
bind SPACE [usenos]

bind UP (getbind W)
bind DOWN (getbind S)
bind RIGHT (getbind D)
bind LEFT (getbind A)